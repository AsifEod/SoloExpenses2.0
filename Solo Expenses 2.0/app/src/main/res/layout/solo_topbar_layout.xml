<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/topbar_layout"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="?attr/ThemePrimaryColor"
    android:visibility="gone"
    tools:background="@color/colorPrimaryDefaultTheme"
    tools:visibility="visible">

    <ImageView
        android:id="@+id/iv_back"
        style="@style/ToolbarElementColor"
        android:layout_width="@dimen/_25sdp"
        android:layout_height="@dimen/_25sdp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="@dimen/_8sdp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:padding="@dimen/_4sdp"
        app:srcCompat="@drawable/back_icon_vector" />

    <TextView
        android:id="@+id/tv_title"
        style="@style/TextViewWhiteLargeStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Screen title"
        android:visibility="gone"
        tools:textColor="@color/white"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="@dimen/_8sdp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_action"
            style="@style/TextViewWhiteLargeStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:text="Submit"
            android:visibility="gone"
            tools:textColor="@color/white"
            tools:visibility="visible" />

        <ImageView
            android:id="@+id/iv_action"
            style="@style/ToolbarElementColor"
            android:layout_width="@dimen/_20sdp"
            android:layout_height="@dimen/_20sdp"
            android:layout_marginStart="@dimen/_8sdp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:visibility="gone"
            app:srcCompat="@drawable/refresh_icon_vector"
            tools:visibility="visible" />

    </LinearLayout>

</RelativeLayout>
